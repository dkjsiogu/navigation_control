# Navigation Control - 全向轮机器人导航系统

基于 ROS2 Humble + Cartographer + 自定义 A* 路径规划的完整导航解决方案。

[![ROS2](https://img.shields.io/badge/ROS2-Humble-blue)](https://docs.ros.org/en/humble/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

## 📦 模块组成

```
navigation_control/
├── launch/                     # 启动文件
│   └── cartographer.launch.py              # 实时建图+导航
├── config/                     # 配置文件
│   ├── cartographer.lua                    # Cartographer实时建图配置
│   └── navigation_debug.rviz               # RViz可视化配置
├── scripts/                    # Python节点
│   ├── astar_planner.py                     # A*路径规划
│   ├── simple_goal_controller.py            # Pure Pursuit控制
│   └── map_republisher.py                   # 地图话题转发
├── src/                        # C++节点
│   ├── wheel_odometry_node.cpp              # 轮式里程计
│   ├── serial_communication.cpp             # 串口接收
│   ├── serial_data_publisher.cpp            # 串口发送
│   └── scan_filter_node.cpp                 # 激光过滤
├── urdf/                       # 机器人模型
│   └── robot.urdf                           # TF静态发布
└── maps/                       # 地图文件
    └── my_map.pbstream                      # Cartographer地图
```

## 🚀 核心功能

### 实时建图导航模式
```bash
ros2 launch navigation_control cartographer.launch.py
```
- **建图**：Cartographer 实时建图，固定起始点 (0,0)
- **定位**：边建图边定位，支持闭环检测
- **路径规划**：自定义 A* 算法 + 矩形机器人足迹碰撞检测
- **路径优化**：插值 → 平滑（20次迭代）→ 曲率简化
- **跟踪控制**：Pure Pursuit + 全向轮运动学
- **数据融合**：RPLIDAR A1 (8Hz) + 轮式里程计 + IMU

## 🛠️ 系统架构

### 节点拓扑
```
┌────────────────────────────────────────────────────────────┐
│                     Cartographer                          │
│  (实时建图 + 定位 + 发布 map→odom TF)                      │
└────────────────────────────────────────────────────────────┘
                           ↓ /map
┌────────────────────────────────────────────────────────────┐
│                   astar_planner.py                        │
│  (矩形footprint碰撞检测 + 圆弧路径生成)                   │
└────────────────────────────────────────────────────────────┘
                           ↓ /planned_path
┌────────────────────────────────────────────────────────────┐
│              simple_goal_controller.py                    │
│  (Pure Pursuit跟踪 + 保持初始朝向)                        │
└────────────────────────────────────────────────────────────┘
                           ↓ /cmd_vel
┌────────────────────────────────────────────────────────────┐
│              serial_data_publisher                        │
│  (速度命令 → 下位机协议转换)                              │
└────────────────────────────────────────────────────────────┘
```

### TF 树
```
map → odom (Cartographer)
      ↓
    base_link (wheel_odometry_node)
      ↓
    laser (URDF静态发布)
```

### 核心节点

| 节点 | 语言 | 功能 |
|------|------|------|
| `wheel_odometry_node` | C++ | 轮式里程计：解析下位机数据 → 发布 `/odom` + TF |
| `serial_communication` | C++ | 串口通信：接收下位机传感器数据 |
| `serial_data_publisher` | C++ | 速度命令转协议：`/cmd_vel` → 下位机格式 |
| `scan_filter_node` | C++ | 激光过滤：过滤机器人本体反射点 |
| `astar_planner.py` | Python | A* 路径规划 + 矩形足迹碰撞 + 平滑简化 |
| `simple_goal_controller.py` | Python | Pure Pursuit 路径跟踪 |
| `map_republisher.py` | Python | 地图话题转发（解决RViz QoS问题）|

## 🔧 硬件要求

- **机器人**：全向轮底盘（262mm × 270mm）
- **激光雷达**：SLAMTEC RPLIDAR A1 (8Hz, 串口)
- **传感器**：3轴IMU（陀螺仪 + 加速度计）
- **下位机**：STM32（串口115200，自定义协议）
- **设备映射**：
  - `/dev/radar` → RPLIDAR
  - `/dev/stm32` → 下位机

## 📦 安装指南

### 系统要求
- **操作系统**：Ubuntu 22.04
- **ROS2 版本**：Humble Hawksbill

### 1. 安装依赖

#### ROS2 软件包
```bash
sudo apt install ros-humble-cartographer-ros \
                 ros-humble-robot-state-publisher \
                 ros-humble-rviz2 \
                 libserial-dev
```

#### RPLIDAR 驱动
```bash
# 克隆官方驱动到工作区
cd ~/ros2_ws/src  # 或你的工作区
git clone https://github.com/Slamtec/sllidar_ros2.git
```

### 2. 克隆本项目
```bash
cd ~/ros2_ws/src
git clone <your-repo-url> navigation_control
```

### 3. 编译
```bash
cd ~/ros2_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install --packages-select navigation_control sllidar_ros2
source install/setup.bash
```

### 4. 配置串口权限
```bash
# 添加用户到 dialout 组
sudo usermod -aG dialout $USER

# 创建 udev 规则（可选，用于固定设备名）
sudo nano /etc/udev/rules.d/99-robot.rules
# 添加以下内容：
# KERNEL=="ttyUSB*", ATTRS{idVendor}=="10c4", ATTRS{idProduct}=="ea60", SYMLINK+="radar"
# KERNEL=="ttyUSB*", ATTRS{idVendor}=="0483", ATTRS{idProduct}=="5740", SYMLINK+="stm32"

# 重新加载规则
sudo udevadm control --reload-rules && sudo udevadm trigger

# 重启生效
```

## 🎮 使用流程

### 启动导航系统
1. **启动完整系统**
   ```bash
   ros2 launch navigation_control cartographer.launch.py
   ```
   - 机器人从 (0,0) 开始实时建图
   - 同时支持导航功能

2. **在 RViz2 中设置目标点**
   - 点击工具栏 "2D Goal Pose"
   - 在地图上点击目标位置
   - 机器人自动规划路径并执行

3. **保存地图（可选）**
   ```bash
   ros2 service call /finish_trajectory cartographer_ros_msgs/srv/FinishTrajectory "{trajectory_id: 0}"
   ros2 service call /write_state cartographer_ros_msgs/srv/WriteState \
     "{filename: '${HOME}/my_map.pbstream'}"
   ```

4. **路径可视化**
   - 实时地图：`/map`（持续更新）
   - 绿色路径：`/planned_path`（优化后的平滑路径）

## ⚙️ 关键参数调优

### 路径规划参数 (`cartographer.launch.py`)
```python
'robot_length': 0.262,          # 机器人长度 (m)
'robot_width': 0.270,           # 机器人宽度 (m)
'safety_margin': 0.03,          # 安全裕量 (m)
'diagonal_penalty': 1.2,        # 对角线惩罚系数
'smoothing_iterations': 20,     # 平滑迭代次数（越大越圆润）
'waypoint_spacing': 0.20,       # 路径点最小间距 (m)
```

### 速度限制参数
```python
'max_linear_vel': 0.5,          # 最大线速度 (m/s)
'max_angular_vel': 0.3,         # 最大角速度 (rad/s)
'goal_tolerance': 0.10,         # 目标容差 (m)
'lookahead_distance': 0.5,      # Pure Pursuit前瞻距离 (m)
```

## 🐛 常见问题

### 1. IMU Yaw 漂移
**现象**：机器人静止时角度缓慢变化  
**原因**：陀螺仪零漂积累  
**解决**：已在 `wheel_odometry_node.cpp` 中实现静止检测，自动冻结IMU更新

### 2. RViz2 消息丢弃警告
**现象**：终端输出 "Message Filter dropping message"  
**原因**：TF队列溢出  
**解决**：已优化 `navigation_debug.rviz` 配置（Depth=1, Best Effort）

### 3. 路径切角
**现象**：转弯时路径过于笔直  
**调优**：增加 `smoothing_iterations` 参数（当前20次）

### 4. 地图构建问题
**解决方法**：
1. 确保机器人从固定位置 (0,0) 启动
2. 建图时保持机器人匀速移动
3. 闭环检测会自动优化地图
4. 如需重新建图，重启节点即可

## 📝 开发指南

- **代码风格**：参考 `.github/copilot-instructions.md`
- **调试工具**：RViz2 实时可视化所有话题
- **日志位置**：`log/latest_build/`

## 🎯 系统特性

✅ **无需 Nav2**：完全自定义导航栈，轻量高效  
✅ **实时建图**：Cartographer 边建图边导航，无需预建图  
✅ **固定起始点**：每次从 (0,0) 启动，坐标系一致  
✅ **闭环检测**：自动优化地图，消除累积误差  
✅ **矩形碰撞检测**：精确的机器人足迹建模  
✅ **圆弧路径生成**：平滑过渡，避免急转弯  
✅ **IMU 融合**：增强角度估计精度  
✅ **实时障碍物过滤**：过滤机器人本体反射

## 📄 许可证

- `sllidar_ros2`: BSD-2-Clause (Slamtec官方)
- `navigation_control`: MIT
- `my_slam_config`: MIT

---

**最后更新**：2025年11月12日  
**ROS2 版本**：Humble Hawksbill  
**机器人平台**：全向轮底盘 + RPLIDAR A1
